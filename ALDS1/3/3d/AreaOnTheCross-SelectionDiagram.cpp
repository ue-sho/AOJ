/*まじでわからん*/
#include <bits/stdc++.h>
using namespace std;
#define rep(i, n) for (int i = 0; i < (n); i++)
int main()
{

    /*実装できていない*/
    stack<int> s1;
    stack<int> s2;

    char ch;
    int i = 1;
    int area = 0;
    while (cin >> ch)
    {
        if (ch == '\\')
        {
            s1.push(i);
        }
        else if (ch == '/')
        {
        }
    }

    return 0;
}